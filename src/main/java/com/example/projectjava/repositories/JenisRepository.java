package com.example.projectjava.repositories;

import com.example.projectjava.models.Jenis;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JenisRepository extends JpaRepository<Jenis, Integer> {
}
